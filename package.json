{
  "name": "aws-weather-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "locally": "node -e \"require('./index.js').handler({}).then(res => console.log(res));\"",
    "awsdeploy": "zip -r weather-lambda.zip . && aws lambda update-function-code --function-name weather-lambda --zip-file fileb://weather-lambda.zip"
  },
  "author": "",
  "license": "ISC"
}
